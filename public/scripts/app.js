'use strict';

/* ========== STATUS ===========
- loading from the net works (the model.detect() method is known) but it is not
possible to model.save() the file to indexedDB
*/


function init() {
  console.log('starting ...')
}

init();
